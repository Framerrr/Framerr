/**
 * LinkedAccountsPage
 * 
 * Wrapper that imports the existing AccountSettings component.
 * 
 * Note: The original AccountSettings.tsx from settings/accounts
 * functionality is imported here.
 */

// Re-export the existing AccountSettings as LinkedAccountsPage
export { AccountSettings as LinkedAccountsPage } from '../../accounts';
