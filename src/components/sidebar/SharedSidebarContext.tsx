// Re-export from new modular location for backwards compatibility
// Original monolithic file split into: SidebarUIContext, SidebarTabsContext, SidebarNavigationContext
export {
    SharedSidebarProvider,
    useSharedSidebar,
    SharedSidebarContext,
    type SidebarMode
} from './context';
