/**
 * Service Monitors Types
 * 
 * Shared types for service monitor routes.
 * Re-exports database types for convenience.
 */

// Re-export database types
export type { ServiceMonitor } from '../../db/serviceMonitors';
